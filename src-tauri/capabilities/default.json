{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Only local avatar and panel windows; native commands own window operations.",
  "windows": ["avatar", "panel"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "notification:default"
  ]
}
