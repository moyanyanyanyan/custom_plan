# DSH 工作文件
.dsh-handoff/
.plugin-src/
.tmp-upscale/
.vfix/
dsh-creative-plugin/
dsh-raw-html-src/
py-libs/
waifu2x-temp/
图标/

# 工具包和图标
*.zip
*.tar.gz
*.ico

# 运维/调试脚本
*.ps1
*.py
read-context*.js
waifu2x-upscale.js

# 会话与数据文件
session-list.json
weisan-*.jsonl

# 依赖与构建产物
node_modules/
dist/
.pnpm-store/
src-tauri/target/

# 本地评审与预览产物
.artifacts/

# 临时目录与文件
modsearch-temp/
*.log

# 密钥与本地配置
.env
*.jpg
*.png
*.jpeg
*.gif

# 例外：角色三视图是编译期内嵌的必需资源（src-tauri/src/ai/stepfun.rs 的 include_bytes!），
# 必须随仓库分发 —— 否则干净检出（clone 后直接编译）会因找不到文件而编译失败。
# 历史上正是因为这条 *.jpg 规则把它们挡在版本控制外，才导致「上线准备」把参考图换成了用户头像。
!src-tauri/assets/*.jpg
